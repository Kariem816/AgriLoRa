# Gateway

Gateway is a simple LoRa node in the network that is also connected to a controller (a PC or laptop) through USB (Serial interface). The gateway job is to receive data from LoRa interface and pass it to the controller and vice versa.