#ifndef AGRI_CMD_H
#define AGRI_CMD_H

#include "../packet.h"

// command IDs are 8 bits, so we can have up to 256 commands
// Each command can have a payload (arguements) of up to 16 bytes, which is enough for most commands

// #define CMD_XYZ 0xW

#endif //AGRI_CMD_H